services:
  pgadmin4:
    image: dpage/pgadmin4:9.13.0
    container_name: pgadmin4
    ports:
      - "5050:80"
    environment:
      PGADMIN_DEFAULT_EMAIL: ${PGADMIN_DEFAULT_EMAIL}
      PGADMIN_DEFAULT_PASSWORD: ${PGADMIN_DEFAULT_PASSWORD}
      PGADMIN_CONFIG_MAX_LOGIN_ATTEMPTS: ${PGADMIN_CONFIG_MAX_LOGIN_ATTEMPTS}
    restart: unless-stopped
    volumes:
      - pgadmin_data:/var/lib/pgadmin

volumes:
  pgadmin_data:

# docker compose --env-file .env.pgadmin -f containers/pgadmin/pgadmin.compose.yml up -d

# recreate from scratch and wipe pgAdmin data
# docker compose down -v
# docker compose up -d

# unlock the existing account without losing data
# docker compose exec pgadmin4 /bin/sh -c 'python3 - <<EOF
# import sqlite3
# db="/var/lib/pgadmin/pgadmin4.db"
# email="danoyekunle6@gmail.com"
# con=sqlite3.connect(db)
# cur=con.cursor()
# cur.execute("UPDATE USER SET LOCKED = 0, LOGIN_ATTEMPTS = 0 WHERE USERNAME = ?", (email,))
# con.commit()
# print("Account unlocked")
# EOF'
